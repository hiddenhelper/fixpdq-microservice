{
  "ENVIRONMENT": "#{ENVIRONMENT}#",
  "AWS_REGION": "#{AWS_REGION}#",
  "AWS_ACCOUNT": "#{AWS_ACCOUNT}#",
  "ACTIONS_LOGGING_LEVEL": "#{ACTIONS_LOGGING_LEVEL}#",
  "ACTIONS_LOGGING_DRIVER": "#{ACTIONS_LOGGING_DRIVER}#",
  "ACTIONS_DB_DYNAMODB": "#{ACTIONS_DB_DYNAMODB}#",
  "COGNITO_USERPOOL_ID": "#{COGNITO_USERPOOL_ID}#",
  "API_ID": "#{API_ID}#",
  "MEMORY_SIZE": "#{MEMORY_SIZE}#",
  "CONCURRENCY": "#{CONCURRENCY}#",
  "TIMEOUT": "#{TIMEOUT}#"
}
